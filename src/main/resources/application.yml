server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/snippets
    username: snippets
    password: snippets
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration